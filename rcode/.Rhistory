for(j in 0:(t-1)){
surv_prob <- surv_prob * P[j]
print(surv_prob-1)
}
sum_comp <- ( g^(t-1) ) * surv_prob * (C^(1-g)) / (1-g)
crra <- crra + sum_comp
}
return(crra)
}
crrautil(10,0.4)
Death <- 110
P <- rep(0.9,Death)
delta <- 0.9
#utility function define:
crrautil <- function(C, g){
crra <- 0
for(t in 0:Death){
surv_prob <- 1
for(j in 0:(t-1)){
surv_prob <- surv_prob*P[j]
}
sum_comp <- ( g^(t-1) ) * surv_prob * (C^(1-g)) / (1-g)
crra <- crra + sum_comp
}
return(crra)
}
crrautil(10,0.4)
P[3]
P[3] * 1
P
x <-1
x <- x * P[3]
x
P[0]
Death <- 110
P <- rep(0.9,Death)
delta <- 0.9
#utility function define:
crrautil <- function(C, g){
crra <- 0
for(t in 1:Death){
surv_prob <- 1
for(j in 1:(t-1)){
surv_prob <- surv_prob * P[j]
}
sum_comp <- ( g^(t-1) ) * surv_prob * (C^(1-g)) / (1-g)
crra <- crra + sum_comp
}
return(crra)
}
crrautil(10,0.4)
Death <- 110
P <- rep(0.9,Death)
delta <- 0.9
#utility function define:
crrautil <- function(C, g){
crra <- 0
for(t in 1:Death-1){
surv_prob <- 1
for(j in 1:(t-1)){
surv_prob <- surv_prob * P[j]
}
sum_comp <- ( g^(t-1) ) * surv_prob * (C^(1-g)) / (1-g)
crra <- crra + sum_comp
}
return(crra)
}
crrautil(10,0.4)
Death <- 110
P <- rep(0.9,Death)
delta <- 0.9
#utility function define:
crrautil <- function(C, g){
crra <- 0
for(t in 1:Death){
surv_prob <- 1
for(j in 1:(t-1)){
surv_prob <- surv_prob * P[j]
print(surv_prob)
}
sum_comp <- ( g^(t-1) ) * surv_prob * (C^(1-g)) / (1-g)
crra <- crra + sum_comp
}
return(crra)
}
crrautil(10,0.4)
Death <- 110
P <- rep(0.9,Death)
delta <- 0.9
#utility function define:
crrautil <- function(C, g){
crra <- 0
for(t in 1:Death){
surv_prob <- 1
for(j in 1:(t-2)){
surv_prob <- surv_prob * P[j]
}
sum_comp <- ( g^(t-1) ) * surv_prob * (C^(1-g)) / (1-g)
crra <- crra + sum_comp
}
return(crra)
}
crrautil(10,0.4)
Death <- 110
P <- rep(0.9,Death)
delta <- 0.9
#utility function define:
crrautil <- function(C, g){
crra <- 0
for(t in 1:Death){
surv_prob <- 1
for(j in 1:(t-1)){
surv_prob <- surv_prob * P[j-1]
}
sum_comp <- ( g^(t-1) ) * surv_prob * (C^(1-g)) / (1-g)
crra <- crra + sum_comp
}
return(crra)
}
crrautil(10,0.4)
Death <- 110
P <- rep(0.9,Death)
delta <- 0.9
#utility function define:
crrautil <- function(C, g){
crra <- 0
for(t in 1:Death){
surv_prob <- 1
for(j in 1:(t-1)){
surv_prob <- surv_prob * P[j+1]
}
sum_comp <- ( g^(t-1) ) * surv_prob * (C^(1-g)) / (1-g)
crra <- crra + sum_comp
}
return(crra)
}
crrautil(10,0.4)
Death <- 110
P <- rep(0.9,Death)
delta <- 0.9
#utility function define:
crrautil <- function(C, g){
crra <- 0
for(t in 1:Death){
surv_prob <- 1
for(j in 1:(t-1)){
surv_prob <- surv_prob * P[j+1]
}
sum_comp <- ( g^(t-1) ) * surv_prob * (C^(1-g)) / (1-g)
crra <- crra + sum_comp
}
return(crra)
}
crrautil(10,0.4)
Death <- 110
P <- rep(0.9,Death)
delta <- 0.9
#utility function define:
crrautil <- function(C, g){
crra <- 0
for(t in 1:Death){
surv_prob <- 1
for(j in 1:(t-1)){
surv_prob <- surv_prob * P[j+1]
}
sum_comp <- ( g^(t-1) ) * surv_prob * (C^(1-g)) / (1-g)
crra <- crra + sum_comp
}
return(crra)
}
crrautil(10,0.4)
Death <- 110
P <- rep(0.9,Death)
delta <- 0.9
#utility function define:
crrautil <- function(C, g){
crra <- 0
for(t in 1:Death){
surv_prob <- 1
for(j in 1:(t-1)){
surv_prob <- surv_prob * P[j+1]
}
sum_comp <- ( g^(t-1) ) * surv_prob * (C^(1-g)) / (1-g)
crra <- crra + sum_comp
}
return(crra)
}
crrautil(10,0.4)
Death <- 110
P <- rep(0.9,Death)
delta <- 0.9
#utility function define:
crrautil <- function(C, g){
crra <- 0
for(t in 1:Death){
surv_prob <- 1
for(j in 1:t{
surv_prob <- surv_prob * P[j]
}
sum_comp <- ( g^(t-1) ) * surv_prob * (C^(1-g)) / (1-g)
crra <- crra + sum_comp
}
return(crra)
}
crrautil(10,0.4)
Death <- 110
P <- rep(0.9,Death)
delta <- 0.9
#utility function define:
crrautil <- function(C, g){
crra <- 0
for(t in 1:Death){
surv_prob <- 1
for(j in 1:t{
surv_prob <- surv_prob * P[j]
}
sum_comp <- ( g^(t-1) ) * surv_prob * (C^(1-g)) / (1-g)
crra <- crra + sum_comp
}
return(crra)
}
crrautil(10,0.4)
Death <- 110
P <- rep(1,Death)
delta <- 0.9
#utility function define:
crrautil <- function(C, g){
crra <- 0
for(t in 1:Death){
surv_prob <- 1
for(j in 1:t{
surv_prob <- surv_prob * P[j]
}
sum_comp <- ( g^(t-1) ) * surv_prob * (C^(1-g)) / (1-g)
crra <- crra + sum_comp
}
return(crra)
}
crrautil(10,0.4)
Death <- 110
P <- rep(1,Death)
delta <- 0.9
#utility function define:
crrautil <- function(C, g){
crra <- 0
for(t in 1:Death){
surv_prob <- 1
for(j in 1:t{
surv_prob <- surv_prob * P[j]
}
sum_comp <- ( g^(t-1) ) * surv_prob * (C^(1-g)) / (1-g)
crra <- crra + sum_comp
}
return(crra)
}
crrautil(10,1)
Death <- 110
P <- rep(1,Death)
delta <- 0.9
#utility function define:
crrautil <- function(C, g){
crra <- 0
for(t in 1:Death){
surv_prob <- 1
for(j in 1:t{
surv_prob <- surv_prob * P[j]
}
sum_comp <- ( g^(t-1) ) * surv_prob * (C^(1-g)) / (1-g)
crra <- crra + sum_comp
}
return(crra)
}
crrautil(10,0.9)
Death <- 110
P <- rep(1,Death)
delta <- 0.9
#utility function define:
crrautil <- function(C, g){
crra <- 0
for(t in 1:Death){
surv_prob <- 1
for(j in 1:t{
surv_prob <- surv_prob * P[j]
}
sum_comp <- ( g^(t-1) ) * surv_prob * (C^(1-g)) / (1-g)
crra <- crra + sum_comp
}
return(crra)
}
crrautil(10,0.5)
Death <- 110
P <- rep(1,Death)
delta <- 0.9
#utility function define:
crrautil <- function(C, g){
crra <- 0
for(t in 1:Death){
surv_prob <- 1
for(j in 1:t{
surv_prob <- surv_prob * P[j]
}
sum_comp <- ( g^(t-1) ) * surv_prob * (C^(1-g)) / (1-g)
crra <- crra + sum_comp
}
return(crra)
}
crrautil(10,0)
Death <- 110
P <- rep(1,Death)
delta <- 0.9
#utility function define:
crrautil <- function(C, g){
crra <- 0
for(t in 1:Death){
surv_prob <- 1
for(j in 1:t{
surv_prob <- surv_prob * P[j]
}
sum_comp <- ( g^(t-1) ) * surv_prob * (C^(1-g)) / (1-g)
crra <- crra + sum_comp
}
return(crra)
}
crrautil(1,0)
Death <- 110
P <- rep(1,Death)
delta <- 0.9
#utility function define:
crrautil <- function(C, g){
crra <- 0
for(t in 1:Death){
surv_prob <- 1
for(j in 1:t{
surv_prob <- surv_prob * P[j]
}
sum_comp <- ( delta^(t-1) ) * surv_prob * (C^(1-g)) / (1-g)
crra <- crra + sum_comp
}
return(crra)
}
crrautil(1,0)
Death <- 110
P <- rep(1,Death)
delta <- 0.9
#utility function define:
crrautil <- function(C, g){
crra <- 0
for(t in 1:Death){
surv_prob <- 1
for(j in 1:t){
surv_prob <- surv_prob * P[j]
}
sum_comp <- ( delta^(t-1) ) * surv_prob * (C^(1-g)) / (1-g)
crra <- crra + sum_comp
}
return(crra)
}
crrautil(1,0)
Death <- 110
P <- rep(1,Death)
delta <- 0.9
#utility function define:
crrautil <- function(C, g){
crra <- 0
for(t in 1:Death){
surv_prob <- 1
for(j in 1:t){
surv_prob <- surv_prob * P[j]
}
sum_comp <- ( delta^(t-1) ) * surv_prob * (C^(1-g)) / (1-g)
crra <- crra + sum_comp
}
return(crra)
}
crrautil(1,0)
Death <- 110
P <- rep(1,Death)
delta <- 1
#utility function define:
crrautil <- function(C, g){
crra <- 0
for(t in 1:Death){
surv_prob <- 1
for(j in 1:t){
surv_prob <- surv_prob * P[j]
}
sum_comp <- ( delta^(t-1) ) * surv_prob * (C^(1-g)) / (1-g)
crra <- crra + sum_comp
}
return(crra)
}
crrautil(1,0)
Death <- 110
P <- rep(1,Death)
delta <- 2
#utility function define:
crrautil <- function(C, g){
crra <- 0
for(t in 1:Death){
surv_prob <- 1
for(j in 1:t){
surv_prob <- surv_prob * P[j]
}
sum_comp <- ( delta^(t-1) ) * surv_prob * (C^(1-g)) / (1-g)
crra <- crra + sum_comp
}
return(crra)
}
crrautil(1,0)
Death <- 110
P <- rep(1,Death)
delta <- 0.5
#utility function define:
crrautil <- function(C, g){
crra <- 0
for(t in 1:Death){
surv_prob <- 1
for(j in 1:t){
surv_prob <- surv_prob * P[j]
}
sum_comp <- ( delta^(t-1) ) * surv_prob * (C^(1-g)) / (1-g)
crra <- crra + sum_comp
}
return(crra)
}
crrautil(1,0)
Death <- 110
P <- rep(1,Death)
delta <- 0.1
#utility function define:
crrautil <- function(C, g){
crra <- 0
for(t in 1:Death){
surv_prob <- 1
for(j in 1:t){
surv_prob <- surv_prob * P[j]
}
sum_comp <- ( delta^(t-1) ) * surv_prob * (C^(1-g)) / (1-g)
crra <- crra + sum_comp
}
return(crra)
}
crrautil(1,0)
Death <- 110
P <- rep(1,Death)
delta <- 1
#utility function define:
crrautil <- function(C, g){
crra <- 0
for(t in 1:Death){
surv_prob <- 1
for(j in 1:t){
surv_prob <- surv_prob * P[j]
}
sum_comp <- ( delta^(t-1) ) * surv_prob * (C^(1-g)) / (1-g)
crra <- crra + sum_comp
}
return(crra)
}
crrautil(1,0)
#utility function define:
crrautil <- function(C, g){
Death <- 110
P <- rep(1,Death)
delta <- 1
crra <- 0
for(t in 1:Death){
surv_prob <- 1
for(j in 1:t){
surv_prob <- surv_prob * P[j]
}
sum_comp <- ( delta^(t-1) ) * surv_prob * (C^(1-g)) / (1-g)
crra <- crra + sum_comp
}
return(crra)
}
crrautil(1,0)
#utility function define:
crrautil <- function(C, g){
Death <- 110
P <- rep(1,Death)
delta <- 1
crra <- 0
for(t in 1:Death){
surv_prob <- 1
for(j in 1:t){
surv_prob <- surv_prob * P[j]
}
sum_comp <- ( delta^(t-1) ) * surv_prob * (C^(1-g)) / (1-g)
crra <- crra + sum_comp
}
return(crra)
}
crrautil(1,0)
rm(list=ls())
crrautil(1,0)
#utility function define:
crrautil <- function(C, g){
Death <- 110
P <- rep(1,Death)
delta <- 1
crra <- 0
for(t in 1:Death){
surv_prob <- 1
for(j in 1:t){
surv_prob <- surv_prob * P[j]
}
sum_comp <- ( delta^(t-1) ) * surv_prob * (C^(1-g)) / (1-g)
crra <- crra + sum_comp
}
return(crra)
}
